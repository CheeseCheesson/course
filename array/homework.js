/*
const peopleWaiting = [
  "Кристина",
  "Олег",
  "Кирилл",
  "Мария",
  "Светлана",
  "Артем",
  "Глеб",
];

function giveParcel(arr) {
  return `${arr.shift()} получил(а) посылку. В очедеди осталось ${
    arr.length
  } человек`;
}

function leaveQueueWithoutParcel(arr) {
  return `${arr.pop()} не получил(а) посылку и ушел(ла) из очереди`;
}

let i = 1;
while (i <= 3) {
  console.log(giveParcel(peopleWaiting));
  i++;
}

let j = peopleWaiting.length;
while (j > 0) {
  console.log(leaveQueueWithoutParcel(peopleWaiting));
  j--;
}

if (!peopleWaiting.length) {
  console.log(`В очереди никого не сталось`);
}
*/
//-------------------------------------------------------------------------------------------------
/*
function getSumOfSequence(number){
    let arr = []

    for(let i = 1; i <= number; i++){
        arr.push(i)
    }

    return arr[0] + arr[arr.length-1]
}
console.log(getSumOfSequence(5))
*/

//-----------------------------------

