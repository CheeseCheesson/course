let arr = []
for(let i = 0; i < 3; i++){
    let temp = []
    arr.push(temp) + '\n'
    for(let j = 1; j <= 5; j++){
        temp.push(j)
    }
}


const matrix = [
    [ 1, 2, 3 ],
    [ 4, 5, 6 ],
    [ 7, 8, 9 ],
];
matrix
const myMatrix = [
    ...matrix[0],
    ...matrix[1],
    ...matrix[2],
]
let clone = []

clone = matrix.reduce((total, item) => {
    total
    item
    return total.concat(item)
})

clone